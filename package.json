{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client",
    "preview": "wrangler -e prod",
    "deploy": "$npm_execpath run build && wrangler deploy -e prod"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^0.1.15",
    "vite": "^6.2.3",
    "vite-plugin-ssr-hot-reload": "^0.3.4",
    "wrangler": "^4.4.0"
  }
}