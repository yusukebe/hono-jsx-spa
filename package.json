{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client",
    "preview": "wrangler dev -e prod",
    "deploy": "$npm_execpath run build && wrangler deploy -e prod"
  },
  "dependencies": {
    "hono": "^4.5.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^0.1.15",
    "@cloudflare/workers-types": "^4.20250321.0",
    "@hono/vite-build": "^1.3.0",
    "vite": "^6.0.11",
    "vite-plugin-ssr-hot-reload": "^0.2.2",
    "wrangler": "^4.4.0"
  }
}